import { registerItemsInSubjectOfArrays } from '../src/registerItemsInSubjectOfArrays';

describe('how registering items in record objects works', () => {
    it('delete me', () => {
        expect(registerItemsInSubjectOfArrays).toBeDefined();
    });

    /*
    TODO:
    it('add items', () => {});

    it('add and remove items', () => {});

    it('add and remove items with sophisticated compare', () => {});

    it('thow when the array is corrupted in the process', () => {});

    it('thow when removing twice', () => {});

    it('add throw error on duplicate items', () => {});

    /*
    TODO:

    it('add ignore duplicate items', () => {});

    it('add override duplicate items', () => {});

    */
});
